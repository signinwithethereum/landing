# Copy to .env.production, which is gitignored. Never commit real values —
# the repository this replaces committed its deploy host and registry user.
DOCKER_REGISTRY_USERNAME=
DEPLOY_HOST=
APP_HOST=siwe.xyz
